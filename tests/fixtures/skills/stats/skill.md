# Skill: stats

You have a tool called `stats`. When the user asks for project statistics, code metrics, or file analysis, call it:

```tool
stats <path>
```

The tool returns structured output with a summary and chart data.
You will receive the summary. Use it to discuss the project's composition.
Do NOT run `find` or `wc` yourself. Always use the `stats` tool.
