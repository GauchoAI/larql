# Skill: search

You have a tool called `search`. When the user asks to find, search, or grep for something, call it:

```tool
search <pattern> <path>
```

The tool returns structured output. You will receive a summary.
Do NOT run `grep` yourself. Always use the `search` tool.
